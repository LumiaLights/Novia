fabricApi {
    configureDataGeneration() {
        client = true
        modId  = project.mod_id
    }
}

addSubmodules(project, ['core-api'])

loom {
    accessWidenerPath = file("src/client/resources/novia-gui-api.accesswidener")
}
